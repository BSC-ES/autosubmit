JOBS:
  INI:
    SCRIPT: 'sleep 0'
    RUNNING: once
    
  SIM:
    SCRIPT: 'sleep 0'
    DEPENDENCIES: INI SIM-1
    RUNNING: once

  ASIM:
    SCRIPT: 'sleep 0'
    DEPENDENCIES: SIM
    RUNNING: once
    SPLITS: 3

  POST:
    SCRIPT: 'sleep 0'
    RUNNING: once
    DEPENDENCIES:
      ASIM:
        SPLITS_FROM:
          2,3:
            SPLITS_TO: 1,2*,3*
    SPLITS: 3
