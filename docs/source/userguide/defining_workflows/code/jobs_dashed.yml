JOBS:
  GET_FILES:
    SCRIPT: 'sleep 0'
    RUNNING: chunk

  IT:
    SCRIPT: 'sleep 0'
    RUNNING: chunk
    QUEUE: debug

  CALC_STATS:
    SCRIPT: 'sleep 0'
    DEPENDENCIES:
      IT:
      GET_FILES:
        STATUS: 'FAILED?'   # run whether GET_FILES failed or finished (completed/skipped)
    RUNNING: chunk
    SYNCHRONIZE: member
